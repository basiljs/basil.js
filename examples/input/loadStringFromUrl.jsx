#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";function draw() {    var posY = 0;  var height = 30;  var gap = 5;    var url = "http://api.twitter.com/1/statuses/user_timeline.rss?screen_name=basil_js";    b.canvasMode(b.MARGIN);  var result = b.loadString(url);  var tweets = result.match(/<title>(.+)<\/title>/g);  for( var i = 1; i < tweets.length; i++) { // skip first title as it is the title of the RSS stream    // pick a random font... this is the one and only time you are allowed to do this... :)    var randomIndex = b.round( b.random(app.fonts.length-1) );     b.textFont(app.fonts[ randomIndex ]);        b.textSize(12);    b.text(tweets[i], 0, posY, b.width, height);    posY = height + gap + posY;      }}b.go();