#include "../../basil.js";

function draw() {
  /*
  var counter = 50;
  var weightStart = 0.25;
  var weightEnd = 11;
  */
 
  b.println("b.width: "+b.width);

  /*
  b.line(0,0, b.width,b.height);

  for (var i = 0; i < counter; i++) {
    var y = b.map(i, 0,counter-1, 0,b.height);
    var weight = b.map(i, 0,counter-1, weightStart,weightEnd);

    b.strokeWeight( 10 );
    b.println(b.width);
    //b.line(0,y, b.width,b.height);
    //b.line(0,0, b.width,10);
  };
  */
};